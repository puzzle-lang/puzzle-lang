package puzzle.sample

fun main() {
    val a = if (true) 1 else 2
    if (1 + 2 == 3) {
        println(true)
    } else {
        println(false)
    }.printLog()
    // val value = if (cursor.match(1)) 1 else { 2 + 3 }
    // println(value)
}